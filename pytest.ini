[pytest]
# Pass -n auto (or -n <N>) at the CLI to enable parallel workers via pytest-xdist.
# Example: pytest -n auto --browser chromium --browser firefox
addopts =
    --browser chromium
    --browser-channel chrome
    --headed
    --log-cli-level=INFO